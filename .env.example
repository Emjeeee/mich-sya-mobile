# Copy this file to .env and fill in the values from the web app's
# .env.local (michael-tasya repo) or Supabase dashboard -> Settings -> API.
# Same Supabase project as the web app -- do not create a new one.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
